# Copy to .env and adjust:
#   cp .env.example .env

# Gemini (required for Assistant — never commit the real key)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite

CORS_ORIGINS=http://localhost:8080
LOG_LEVEL=WARNING
FRONTEND_PORT=8080
